# 14. `unsafe`: интерпретация памяти

> Цель главы: Отличать числовую конверсию от интерпретации памяти.

```go
// import "unsafe"
bits := uint32(0x3f800000)
number := *(*float32)(unsafe.Pointer(&bits)) // 1 на соответствующем представлении
```

Это чтение битов через другой тип, не числовое преобразование `float32(bits)`. Для такого примера лучше готовая функция `math.Float32frombits`.

`unsafe.Pointer` допускает переходы к/от типизированных указателей и `uintptr` при документированных ограничениях. `uintptr` — целое число, не GC-ссылка: нельзя хранить в нём адрес и считать объект защищённым от сборки мусора. Арифметика адресов и обратные преобразования допустимы только в перечисленных документацией шаблонах.

| Версия | Операции |
|---|---|
| Ранний Go | `unsafe.Pointer`, ограниченные переходы к/от `uintptr` |
| Go 1.17 | `unsafe.Add`, `unsafe.Slice` |
| Go 1.20 | `unsafe.String`, `unsafe.StringData`, `unsafe.SliceData` |

Эти функции позволяют строить представления общей памяти. Необходимо соблюдать время жизни, размер и выравнивание, а также запрет изменения байтов строки. Старые приёмы через ручное создание `reflect.StringHeader`/`SliceHeader` не стоит использовать как образец. Источник: [unsafe](https://pkg.go.dev/unsafe).

## Самопроверка

Почему uintptr не удерживает объект от сборки мусора?

---

[← Развитие дженериков после Go 1.20](13-modern-generics.md) · [Оглавление](../README.md) · [Соседние механизмы, которые тоже называют приведением →](15-adapters.md)
