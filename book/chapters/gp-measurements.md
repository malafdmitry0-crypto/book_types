# 24. Измерения: стоимость полного вызова и самого обхода

> Сравним одинаковую работу и сохраним сырые результаты, чтобы вывод можно было проверить.

Код главы: [benchmark_test.go](../../lessons/journey/benchmark_test.go) · [сырой вывод](../../lessons/journey/results/bench-go1.25.4.txt) · [окружение](../../lessons/journey/results/environment.txt) · [расчёт таблиц](../../scripts/summarize-journey.mjs).

## Что именно измеряется

Все варианты получают 64 дроби 1/3. Исходный []Fraction создаётся до таймера. Время упаковки в []interface{}, проверки reflection-сигнатур, извлечения результата и материализации Filter входит в измерение полного вызова. Каждый результат сохраняется, ошибки проверяются.

| Измерение | Работа одного вызова |
|---|---|
| FindCheap | Полный проход; Validate и дешёвая проверка числителя на 7; совпадений нет |
| FindMath | Полный проход; Validate и математическое Equal(1/2); совпадений нет |
| Sum | Все 64 сложения с initial=0/1; результат 64/3 |
| Filter | Проверка положительности всех 64 дробей и построение полного результата |

В FindCheap мы специально упрощаем предикат, чтобы лучше увидеть стоимость передачи управления. Это отдельное измерение механизма, а не замена математического поиска из сквозного сценария. FindMath возвращает исходный предметный смысл сравнения.

## Условия запуска

Go 1.25.4, darwin/arm64, Apple M1 Pro. Для каждого случая сделано три коротких измерения с benchtime=100ms и GOMAXPROCS=1 через -cpu=1. Это не закрепление на физическом ядре и не фиксация частоты процессора. Таблицы показывают медианы, для дешёвого поиска также указан диапазон.

```sh
go test ./lessons/journey -run '^$' -bench 'BenchmarkJourney|BenchmarkAnyPrepared' -benchmem -benchtime=100ms -count=3 -cpu=1
```

ns/op — среднее время одного полного вызова внутри замера, а не одного элемента. B/op и allocs/op относятся к памяти, выделенной на вызов. Механика benchmark описана в [документации testing](https://pkg.go.dev/testing#hdr-Benchmarks).

## Полученные результаты

Первая таблица — FindCheap: дешёвый предикат по числителю, где вес способа вызова виден лучше всего.

| Подход | Медиана, ns/op | Диапазон, ns/op | B/op | allocs/op |
|---|---|---|---|---|
| concrete | 166.8 | 158.9–280.4 | 0 | 0 |
| closure | 137 | 135.3–138.2 | 0 | 0 |
| interface | 218.5 | 213–247.1 | 32 | 1 |
| empty-interface | 1379 | 1325–1385 | 2176 | 65 |
| reflection | 11859 | 11755–12338 | 4184 | 193 |
| generation | 150.5 | 150–150.7 | 0 | 0 |
| generic | 153.7 | 153.3–162.6 | 0 | 0 |
| iterator | 179.6 | 177.2–180.3 | 0 | 0 |

| Подход | FindMath, µs/op | Sum, µs/op | Filter, µs/op |
|---|---|---|---|
| concrete | 21.27 | 33.54 | 14.00 |
| closure | 19.33 | 32.46 | 14.00 |
| interface | 19.46 | 33.00 | 13.92 |
| empty-interface | 21.31 | 35.27 | 15.34 |
| reflection | 33.33 | 49.89 | 31.31 |
| generation | 19.40 | 33.44 | 13.88 |
| generic | 19.19 | 33.05 | 13.63 |
| iterator | 19.68 | 32.64 | 15.55 |

AnyFind с заранее подготовленным входом: медиана 144.2 ns/op, диапазон 143.3–144.8, 0 B/op, 0 allocs/op.

## Как читать эти числа

В дешёвом поиске упаковка 64 дробей в интерфейсный срез заметна. Когда boxed-срез подготовлен заранее, AnyFind выполняет гораздо меньше работы: упаковка уже оплачена вне таймера. Поэтому 144.2 ns/op подготовленного ядра нельзя сопоставлять с полной стоимостью другого API и называть выигрышем потребителя.

В FindMath и Sum значительную часть работы выполняют методы Fraction, использующие math/big. Различия способов вызова оказываются на фоне более дорогой предметной операции. Это видно при сопоставлении дешёвого и математического предикатов; это не доказательство одинаковой стоимости любых callback.

У конкретного FindCheap разброс составил 158.9–280.4 ns/op. Уже поэтому мелкие различия между concrete, generation, generic и closure не следует превращать в рейтинг. Три коротких запуска — учебный снимок, не статистическое доказательство превосходства.

Интерфейсный вариант в этом стенде выделяет память под конкретный адаптер. Это наблюдение для данного кода и компилятора, а не правило «любой интерфейс всегда аллоцирует». Аналогично результат reflection включает проверку сигнатуры и вызовы reflect.Value.Call; другие reflection-API могут иметь иной способ подготовки.

## Что включено в цену удобства

Стенд Approach использует поля-функции, чтобы одинаково вызывать варианты. Эта диспетчеризация есть в полном измерении. Мы измеряем конкретный путь потребителя из сквозного сценария, а не изолированную машинную инструкцию вызова интерфейсного метода.

Итераторный Filter полностью потребляется и собирается в срез. Иначе сравнение с eager Filter было бы некорректным. Для задачи «получить только первый подходящий элемент» нужен другой benchmark с ранним завершением, а не перенос этих цифр.

## Как повторить исследование

1. Сначала запустите тесты общего договора: сравниваемые реализации должны выдавать одинаковые результаты.
2. Повторите измерения на своей машине с теми же параметрами и сохраните полный вывод.
3. Затем отдельно измените размер входа, положение совпадения или стоимость callback.
4. Для производственного решения увеличьте длительность и число повторов, оцените разброс и условия запуска.

[Скрипт таблиц](../../scripts/summarize-journey.mjs) читает сохранённый файл замера, проверяет наличие трёх повторов каждого случая и вычисляет медианы. Он не запускает benchmark и не подставляет выдуманные значения.

## Проверка понимания

Почему у AnyPrepared ноль аллокаций, хотя перед ним был создан интерфейсный срез? Как изменился бы вывод, если сравнивать ленивый Filter без потребления с уже материализованным результатом?

---

[← Filter и Reduce: один договор для разных API](gp-filter-reduce.md) · [Оглавление](../README.md) · [Практикум: задания, решения и fuzz-проверки →](gp-practicum.md)
