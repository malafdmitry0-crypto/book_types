# 3. База: `T(x)` и числовые типы

Код главы: [example_test.go](../../lessons/reference/basics/example_test.go) · [failures_test.go](../../lessons/failures/failures_test.go).


> Цель главы: Предсказывать результат числовой конверсии.

Преобразуем несколько чисел и заранее предскажем результат. Особенно важно различать потерю дробной части, сужение целого и ошибку компиляции для непредставимой константы.

```go
var n int = 42
wide := int64(n)
fraction := float64(n)

var f float64 = -3.9
whole := int(f) // -3: дробная часть отбрасывается к нулю

var big uint16 = 300
small := uint8(big) // 44: остаются младшие 8 бит

var negative int = -1
unsigned := uint8(negative) // 255

var z complex128 = 3 + 4i
compact := complex64(z)
realPart := real(z)
imagPart := imag(z)
constructed := complex(float64(n), 0)

// var bad int64 = n // разные типы: нужна int64(n)
// _ = int(true)    // bool → число не предусмотрено
// _ = bool(1)      // число → bool не предусмотрено
// _ = float64(z)   // сначала явно выбираем real(z) или imag(z)
```

Целые и вещественные значения можно преобразовывать друг в друга; комплексные — между комплексными типами. Для перехода к комплексному числу из переменной используем `complex`, для выделения частей — `real` и `imag`. Константы рассматриваются отдельно.

Сужение целого не возвращает ошибку переполнения. Переход к floating-point может терять точность. Если результат преобразования с float/complex не представим целевым типом, нельзя полагаться на переносимый результат. Размер `int` зависит от платформы.

Полный нормативный перечень разрешённых конверсий: [спецификация, Conversions](https://go.dev/ref/spec#Conversions).

## Самопроверка

Почему uint8(300) запрещено, а преобразование переменной со значением 300 возможно?

---

[← История возможностей](02-history.md) · [Оглавление](../README.md) · [Константы и присваивание →](04-assignment.md)
